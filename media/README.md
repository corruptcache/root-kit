🕹️ Emulation & Media Management ToolsThis directory contains the core logic for the Exodia automated media pipeline. These scripts are designed to be run either natively (if dependencies are met) or inside the Exodia Toolkit container.📋 General LogicAll scripts in this suite share a common "DNA" for safety and consistency:Verify-First: No source file is deleted until the output file passes a hash integrity check (chdman verify or dolphin-tool verify).Recursive by Default: Scripts will dive into subfolders unless told otherwise (-f).Dry Run Supported: Always use -n first to see what would happen.Self-Cleaning: If a conversion fails, the script cleans up the partial/corrupt output file automatically.💿 CHD Manager (chd_manager.sh)Purpose: Lifecycle management for optical media (CD/DVD) using chdman.Target Systems: PS1, PS2, Sega CD, Saturn, Dreamcast, PC Engine CD.Auto-Excludes: GameCube, Wii, Wii U, Xbox (Prevents accidental conversion of formats better served by RVZ/ISO).UsageBash./chd_manager.sh -d <TARGET_DIRECTORY> [OPTIONS]
Modes (-m)ModeDescriptionLogicconvert(Default) ISO/CUE $\to$ CHDCreates CHD (using createcd), verifies, then deletes source.revertCHD $\to$ ISO/BINExtracts CHD back to raw format. Attempts extractcd first, then extractdvd.repairFix Bad HeadersPS2 Specific. Extracts "Bad" CHDs (created with createdvd) to temp ISO and re-encodes them correctly with createcd.verifyAudit LibraryRuns chdman verify on all existing CHD files. Logs errors.cleanupDeduplicationScans for duplicates (e.g., if you have Game.chd, it deletes Game.iso/Game.zip).FlagsFlagFunctionDefault-dDirectory. The root path to scan.Required-nDry Run. Simulation mode. Prints actions to log without changes.false-fFlat Scan. Disables recursion (processes only the specific folder).false (Recursive)-lLog File. Custom path for the operation log./var/log/chd_manager.logExamplesBash# Convert all PS2 games, recursively
./chd_manager.sh -d /mnt/user/Games/PS2

# Fix a PS2 library that isn't loading in NetherSX2
./chd_manager.sh -d /mnt/user/Games/PS2 -m repair

# Check for duplicates in Dreamcast folder without deleting anything
./chd_manager.sh -d /mnt/user/Games/Dreamcast -m cleanup -n
🐬 RVZ Manager (rvz_manager.sh)Purpose: Compression management for Nintendo GameCube and Wii using dolphin-tool.Target Systems: GameCube, Wii.Compression Settings: RVZ Format, ZStandard (zstd), Compression Level 5 (Optimal speed/size balance).UsageBash./rvz_manager.sh -d <TARGET_DIRECTORY> [OPTIONS]
Modes (-m)ModeDescriptionLogicconvert(Default) ISO/GCM $\to$ RVZConverts to RVZ, verifies integrity, removes NKit junk, deletes source.revertRVZ $\to$ ISODecodes RVZ back to a pristine 1:1 ISO.verifyAudit LibraryRuns dolphin-tool verify on all existing RVZ files.cleanupDeduplicationDeletes redundant .iso, .gcm, or .zip files if a valid .rvz exists.FlagsFlagFunctionDefault-dDirectory. The root path to scan.Required-nDry Run. Simulation mode.false-fFlat Scan. Disable recursion.false (Recursive)-lLog File. Custom log path./var/log/rvz_manager.logExamplesBash# Compress entire GameCube collection
./rvz_manager.sh -d /mnt/user/Games/GameCube

# Convert a Wii library, verify it, and clean up duplicates in one go
./rvz_manager.sh -d /mnt/user/Games/Wii -m convert
./rvz_manager.sh -d /mnt/user/Games/Wii -m cleanup
⚠️ Troubleshooting / FAQQ: chdman or dolphin-tool not found? A: These scripts do not include the binaries. They expect to be run inside the Exodia Toolkit container, or on a host where mame-tools and dolphin-emu are installed in the system $PATH.Q: My PS2 game says "File not found" in the emulator after conversion? A: You likely used the createdvd command in an older script. Run chd_manager.sh -m repair on that folder to fix the header.Q: Can I run this on my entire /Games root? A: Yes, but careful. ./chd_manager.sh -d /mnt/user/Games will scan everything.It will skip GameCube/Wii folders (safety exclusion).It will convert PS1, PS2, SegaCD, etc.Recommendation: Run one system at a time to keep logs clean.Ultimate ROM File Compression Guide (CHD, PBP, and RVZ)This video provides a visual breakdown of why CHD and RVZ are the superior formats for modern emulation.
